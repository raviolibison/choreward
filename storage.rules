rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /proof/{householdId}/{fileName} {
      // Any authenticated user can upload/read proof photos
      allow read, write: if request.auth != null;
    }
  }
}
